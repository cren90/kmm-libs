// Ideally this will only contain versions used in plugins. For dependency only versions, prefer the
// DependencyVersions object in settings.gradle.kts
extra.set("kotlinVersion", "1.6.21")
extra.set("gradleVersion", "7.3.0-beta01")
